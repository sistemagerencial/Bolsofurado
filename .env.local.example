# Exemplo de configuração local para desenvolvimento
# COPIE este arquivo para `.env.local` e preencha os valores reais.
# Nunca comite o arquivo `.env.local` nem exponha a `SERVICE_ROLE` publicamente.
#
# Instruções:
# 1) Copie este arquivo para `.env.local`:
#    copy .env.local.example .env.local   (PowerShell/Windows)
# 2) Abra `.env.local` e substitua os placeholders pelas suas chaves.
# 3) Carregue as variáveis na sessão PowerShell quando for testar:
#    $env:VITE_SUPABASE_URL = (Get-Content .env.local | Select-String '^VITE_SUPABASE_URL=').Line -replace '^[^=]*=','' -replace '"','' ; $env:VITE_SUPABASE_URL = $env:VITE_SUPABASE_URL.Trim().TrimEnd('/')
#    $env:VITE_SUPABASE_ANON_KEY = (Get-Content .env.local | Select-String '^VITE_SUPABASE_ANON_KEY=').Line -replace '^[^=]*=','' -replace '"','' ; $env:VITE_SUPABASE_ANON_KEY = $env:VITE_SUPABASE_ANON_KEY.Trim()
#    $env:SERVICE_ROLE = (Get-Content .env.local | Select-String '^SERVICE_ROLE=').Line -replace '^[^=]*=','' -replace '"','' ; $env:SERVICE_ROLE = $env:SERVICE_ROLE.Trim()
#
# Segurança:
# - Nunca envie `.env.local` para o controle de versão.
# - Use o `SERVICE_ROLE` apenas em ambientes de servidor ou para testes locais seguros.

# Valores de exemplo (substitua pelos seus valores locais)
VITE_SUPABASE_URL=https://oepcnqqlcgnjtosaxsru.supabase.co
VITE_SUPABASE_ANON_KEY=PAeyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im9lcGNucXFsY2duanRvc2F4c3J1Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzAwNTA1MDMsImV4cCI6MjA4NTYyNjUwM30.u39GTo9SJWdGzwdi7W8DBF_w06_rqNQlJpMs05kNkZ4
SERVICE_ROLE=PASTE_YOUeyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im9lcGNucXFsY2duanRvc2F4c3J1Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3MDA1MDUwMywiZXhwIjoyMDg1NjI2NTAzfQ.UDot9g_XX17XZ3TscUnsmtxoczUFV-ZZOoKIXewpJuc
